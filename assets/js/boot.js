/* Loaded synchronously in <head>.
   Marks the document as JS-capable so the reveal animations can start hidden.
   If JavaScript is disabled this never runs, and every section stays visible. */
document.documentElement.classList.add('js');
